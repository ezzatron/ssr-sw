export function createRouter (routes) {
  return {
    build (name, params = {}) {
    },

    nodes (toName, fromName = '') {
    },

    path (toName, fromName = '') {
    },

    resolve (url) {
    },

    routes,
  }
}
